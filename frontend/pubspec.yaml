name: hotel_booking
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # HTTP client for the API repositories (lib/repositories/api/).
  http: ^1.2.2
  # Media types for multipart slip uploads (transitive dep of http, pinned direct).
  http_parser: ^4.0.2

  # State management
  flutter_bloc: ^9.1.1
  # Date / currency formatting
  intl: ^0.20.2
  # Unique IDs for bookings/rooms
  uuid: ^4.5.1
  # Launch Google Maps (location + directions) and external links
  url_launcher: ^6.3.1
  # Lightweight local persistence for the signed-in session
  shared_preferences: ^2.3.2
  # Pick images from the device gallery/camera or computer file browser
  image_picker: ^1.1.2
  # Embeds the resort's Google Maps location (Profile > Hotel location)
  webview_flutter: ^4.10.0
  webview_flutter_web: ^0.2.3+4
  sentry_flutter: ^9.30.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Uploads the Dart obfuscation map and split debug info to Sentry. Without
  # it, a release APK built with --obfuscate reports crashes as meaningless
  # symbol soup. Run `dart run sentry_dart_plugin` immediately after the
  # build, in the same job — see the `sentry:` block below.
  sentry_dart_plugin: ^3.2.1

# Config for sentry_dart_plugin (see dev_dependencies above).
# The auth token is deliberately absent: the plugin reads SENTRY_AUTH_TOKEN
# from the environment, and unlike the DSN that token DOES grant write access
# to the project — it must never be committed or compiled into the app.
sentry:
  org: tourist-booking-app
  project: mobile-app
  upload_debug_symbols: true
  upload_sources: true
  # Must match --split-debug-info in the build command. Left unset the plugin
  # scans the whole project directory ('.') instead.
  symbols_path: build/debug-info
  # A FILE, not a directory, and it has no default: without it the plugin logs
  # a warning and skips the obfuscation map entirely — which is the one
  # artifact that turns obfuscated Dart frames back into readable names.
  # Must match --save-obfuscation-map in the build command.
  dart_symbol_map_path: build/app/obfuscation.map.json
  # Web-only, and CI builds an APK. Leaving it on makes the plugin look for a
  # build/web that was never produced.
  upload_source_maps: false
  # Block until Sentry has processed the upload, so a rejected artifact fails
  # the CI step instead of passing quietly.
  wait_for_processing: true
  # Default is 'auto', which runs `sentry-cli releases set-commits --auto` and
  # needs a GitHub repo integration configured in Sentry. Turn this on once
  # that exists — it is what makes suspect commits work.
  commits: 'false'

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - image/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
